# CHANGELOG

## [0.1.0]

- Initial release. `LiveTennisAPIToolSpec` wraps the official `livetennisapi`
  client and exposes live matches, upcoming matches, match detail, player search,
  player profiles, fixtures, recent results, match events, market odds, model
  analysis and an API status/plan check as LlamaIndex tools.
